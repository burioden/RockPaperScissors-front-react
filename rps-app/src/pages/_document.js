import { Html, Head, Main, NextScript } from "next/document";
// import { Kiwi_Maru, Khand } from "next/font/google";

// const Kiwi_Maru = Kiwi_Maru({ weight: "400", subsets: ["latin"] });
// const Khand = Khand({ weight: "400", subsets: ["latin"] });

export default function Document() {
  return (
    <Html lang="ja">
      <Head />
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
